import { Routes } from '@angular/router';

import {
  CaseStudySignals,
} from './components/case-study-signals/case-study-signals';

export const routes: Routes = [
    {
        path: '/signals',
        component: CaseStudySignals
    }
];
